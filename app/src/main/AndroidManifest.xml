<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectofinalandroid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoFinalAndroid">
        <activity android:name=".Vista.ViewRegistroClase"></activity>
        <activity android:name=".Vista.ViewClases" />
        <activity android:name=".Vista.ListadoForos" />
        <activity android:name=".Vista.ViewRegistroForo" />
        <activity android:name=".Vista.ViewEstudiante" />
        <activity android:name=".Vista.ViewDocente" />
        <activity android:name=".Vista.ViewRegistroDocente" />
        <activity android:name=".Vista.ViewRegistroEstudiante" />
        <activity android:name=".Vista.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>